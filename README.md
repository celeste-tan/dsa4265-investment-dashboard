# Instructions

## Backend

- `cd backend`
- If venv does not exist:
    - `python -m venv venv`
    - `source venv/bin/activate`
    - `pip install -r ../requirements.txt`
- Else:
    = `source venv/bin/activate`
- `python app.py`
